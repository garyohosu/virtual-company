# 📚 Virtual Company - CLI エージェント 設定ガイド

**目的**: このフォルダ内のファイルを実行すること

---

## 🎯 このフォルダ（instructions/）の役割

```
garyohosu/virtual-company/instructions/

👆 この フォルダ 内のすべてのファイルが：
   ✅ 自動実行されるファイル
   ✅ git pull で新ファイルが追加されたら実行
   ✅ CLI ツール（Gemini など）が自動検出して実行
```

---

## 📝 このフォルダのファイル説明

### 🔴 設定ファイル（ルール定義・実行しない）

これらのファイルは **ルール定義**なので実行しません。読むだけです：

```
Agents.md           ← すべてのエージェントが従うべき統一ルール
Claude.md           ← Claude の役割と責務
Gemini.md           ← Gemini CLI の役割と責務
CLAUDE_MEMORY.md    ← Claude の永続記憶
README_instructions.md ← このフォルダの説明
```

### 🟢 実行ファイル（実際に実行される）

これらのファイルは **自動実行**されます：

```
SETUP_*.md / SETUP_*.sh           ← セットアップ（最初に実行）
MAGICBOXAI_FINAL_SETUP.md         ← MagicBoxAI セットアップ
order_*.md / order_*.sh           ← 標準指示スクリプト（100+ 個）
TEST_*.sh                         ← テスト実行
VERIFY_*.sh                       ← 検証スクリプト
cleanup_organize_files.sh         ← ファイル整理
... その他
```

---

## 🚀 実行フロー

```
1️⃣ ユーザーが git pull を実行
   └─ cd ~/garyohosu/virtual-company && git pull origin main

2️⃣ CLI ツール（Gemini など）が自動起動
   └─ このフォルダをスキャン

3️⃣ CLI が新しいファイルを検出
   ├─ order_xxx.sh
   ├─ order_xxx.md
   └─ TEST_xxx.sh など

4️⃣ CLI が実行
   ├─ .sh ファイル → bash で実行
   ├─ .md ファイル → 読む or 解析
   └─ 順序: SETUP > order > TEST

5️⃣ 完了を記録
   └─ .last_agent_run を更新
   └─ Git にプッシュ
```

---

## ✅ 設定ファイルを読む順序

```
1️⃣ README_instructions.md（このファイル）
   └─ フォルダの概要を理解

2️⃣ Agents.md
   └─ すべてのエージェントが従うルール

3️⃣ Claude.md （if you are Claude）
   └─ 指示書の作成方法

3️⃣ Gemini.md （if you are Gemini CLI）
   └─ 実行方法の詳細

4️⃣ CLAUDE_MEMORY.md
   └─ 重要な制約情報を確認
```

---

## 📍 重要なリンク

| 項目 | リンク |
|------|--------|
| Agents ルール | [Agents.md](https://github.com/garyohosu/virtual-company/blob/main/instructions/Agents.md) |
| Claude 設定 | [Claude.md](https://github.com/garyohosu/virtual-company/blob/main/instructions/Claude.md) |
| Gemini 設定 | [Gemini.md](https://github.com/garyohosu/virtual-company/blob/main/instructions/Gemini.md) |
| Claude 永続記憶 | [CLAUDE_MEMORY.md](https://github.com/garyohosu/virtual-company/blob/main/instructions/CLAUDE_MEMORY.md) |

---

## 🎯 このフォルダの「使い方」

### For Claude（AI エージェント）

```
1. Claude.md を読む
2. 指示書を作成する
3. このフォルダ（instructions/）に保存
4. GitHub にプッシュ
5. ユーザーが git pull
6. Gemini が自動実行
```

### For Gemini CLI（CLI ツール）

```
1. Gemini.md を読む
2. 起動時にこのフォルダをスキャン
3. 新しいファイルを検出
4. Agents.md のルール に従って実行
5. 完了を記録して git push
```

### For ユーザー

```
1. このフォルダの役割を理解
2. git pull を実行
3. CLI ツールが自動実行する
4. 完了を確認
```

---

**このフォルダ内のすべてのファイルが Virtual Company の「心臓」です！** 🫀✨
