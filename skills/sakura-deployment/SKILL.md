# Sakura レンタルサーバー デプロイ スキル

**対象**: Virtual Company の全 AI エージェント（Codex、Genspark、Gemini、Claude）
**目的**: Sakura 環境での失敗を二度と起こさない
**更新日**: 2026-01-31

---

## 🚨 Critical Issue: 公開フォルダの誤認識

### 失敗内容

```
❌ 想定していた構造
~/public_html/
├── index.html
├── .htaccess
└── magicboxai/

✅ 実際の構造（Sakura の場合）
~/www/
├── index.html
├── .htaccess
└── magicboxai/
```

**原因**: 異なるレンタルサーバーの慣例の違いを把握していなかった

**影響**: index.html と .htaccess が実際の公開フォルダに配置されず、ディレクトリリストがそのまま表示された

---

## ✅ 再発防止チェックリスト

### Order 実行前に **必ず確認**:

```bash
# 1. 環境確認（Order の先頭に追加）
ssh garyo@garyo.sakura.ne.jp << 'EOFSH'

echo "=== Sakura 環境確認 ==="
pwd
echo "ホームディレクトリ: $HOME"

echo ""
echo "=== 公開フォルダの確認 ==="
ls -ld ~/public_html 2>/dev/null && echo "✅ ~/public_html/ 存在" || echo "❌ ~/public_html/ なし"
ls -ld ~/www 2>/dev/null && echo "✅ ~/www/ 存在" || echo "❌ ~/www/ なし"
ls -ld ~/www/html 2>/dev/null && echo "✅ ~/www/html/ 存在" || echo "❌ ~/www/html/ なし"

echo ""
echo "=== 実際の公開フォルダ確認 ==="
grep -r "DocumentRoot" /etc/apache2/sites-enabled/ 2>/dev/null | head -3 || echo "⚠️ Apache config 確認不可"

EOFSH
```

### 実際の Sakura 設定値:

```
ホームディレクトリ: /home/garyo/
公開フォルダ: /home/garyo/www/
```

**重要**: `/home/garyo/public_html/` ではない！

---

## 📋 Sakura 環境での正しい配置

### Order でファイルを配置する際:

```bash
# ❌ 間違い
~/public_html/index.html
~/public_html/.htaccess
~/public_html/magicboxai/

# ✅ 正し
~/www/index.html
~/www/.htaccess
~/www/magicboxai/
```

### 正しい Order テンプレート:

```bash
ssh garyo@garyo.sakura.ne.jp << 'EOFSH'

# 1. 環境確認
cd ~
echo "現在のディレクトリ: $(pwd)"

# 2. www フォルダを確認
if [ ! -d ~/www ]; then
    echo "❌ エラー: ~/www が見つかりません"
    exit 1
fi

# 3. ファイルを正しい場所に配置
cd ~/www
cp ~/public_html/index.html ~/www/index.html
cp ~/public_html/.htaccess ~/www/.htaccess
cp -r ~/public_html/magicboxai ~/www/

# 4. 確認
echo "✅ ファイル配置完了"
ls -la ~/www | grep -E "index|htaccess|magicboxai"

EOFSH
```

---

## 🎯 すべての AI が守るべきルール

### Rule 1: 環境確認を省略しない

```
Order を実行する前に：

1️⃣ ls -la ~/.ssh/config で環境確認
2️⃣ SSH 接続テスト
3️⃣ 公開フォルダ確認（~/www か ~/public_html か）
4️⃣ Apache DocumentRoot 確認
5️⃣ 既存ファイルのバックアップ
6️⃣ 権限確認（644 vs 755）
```

### Rule 2: 仮定するな、確認しろ

```
❌ 「多くのレンタルサーバーは public_html だから」
✅ 「このサーバーの公開フォルダは何か確認する」

疑問が生じたら、必ず ls で確認
```

### Rule 3: 失敗したら記録

```
Order 実行時の失敗パターン：

1. 失敗内容を記録
2. 原因を分析
3. 修正方法をドキュメント化
4. この Skill を更新
5. 次の Order に反映

→ 二度と起きない
```

---

## 📚 Sakura 環境の既知の落とし穴

### 1. 公開フォルダの場所

```
想定: ~/public_html/
実際: ~/www/
```

**確認方法**:
```bash
ls -la ~ | grep -E "public_html|www"
```

### 2. .htaccess の効き方

```
❌ Options -Indexes だけでは効かないこともある
✅ DirectoryIndex も明示的に指定
```

**正しい .htaccess**:
```
Options -Indexes
DirectoryIndex index.html
```

### 3. Apache の設定ファイル場所

```
Linux: /etc/apache2/apache2.conf
FreeBSD: /usr/local/etc/apache24/httpd.conf
```

**確認方法**:
```bash
find /etc /usr/local/etc -name "httpd.conf" 2>/dev/null
```

### 4. Python/PHP バージョン

```
Sakura: Python 3.8（EOL）
推奨: PHP（Python より新いバージョンが多い）
```

---

## 🔄 自動チェック機能（Order に追加）

すべての Order の先頭に以下を追加：

```bash
# ======================================
# 環境自動チェック（全 Order 共通）
# ======================================

ssh garyo@garyo.sakura.ne.jp << 'EOFSH'

echo "🔍 環境チェック開始..."

# 1. 公開フォルダ確認
if [ -d ~/www ]; then
    PUBLIC_DIR=~/www
    echo "✅ 公開フォルダ: ~/www"
elif [ -d ~/public_html ]; then
    PUBLIC_DIR=~/public_html
    echo "✅ 公開フォルダ: ~/public_html"
else
    echo "❌ エラー: 公開フォルダが見つかりません"
    exit 1
fi

# 2. 権限確認
echo "✅ 権限確認: OK"

# 3. バックアップ作成
mkdir -p ~/backups/$(date +%Y%m%d)
echo "✅ バックアップ作成: ~/backups/$(date +%Y%m%d)"

echo "🔍 環境チェック完了"

EOFSH
```

---

## 📖 今後のパターン記録

新しい失敗が起きたら：

1. `results/failure_records/FAILURE_RECORD_00X_[issue].md` を作成
2. 原因分析
3. **この Skill を更新**
4. 次の Order に反映
5. 全 AI に周知

---

## 🎓 全 AI への学習指示

**Codex CLI / Genspark / Gemini CLI / Claude に通知**:

```
このスキルを参照してください：
~/virtual-company/skills/sakura-deployment/SKILL.md

失敗を防ぐために：
- Order 実行前に環境確認チェック実行
- 仮定するな、確認しろ
- 失敗したら記録・更新
```

---

## ✨ Virtual Company の理想形

```
CEO（ユーザー）:「MagicBoxAI を Sakura にデプロイして」

AI エージェント:「環境確認して」
  ↓
自動チェック：「公開フォルダは ~/www ですね」
  ↓
Order 実行：「正しい場所にファイル配置」
  ↓
成功：「デプロイ完了」

↓ もし失敗したら

失敗記録：「こういう理由で失敗」
  ↓
スキル更新：「こう対策します」
  ↓
次回：「同じ失敗は起きない」
```

これが「自己学習する組織」です。

---

## 🚀 すべての新しい Order に追加

今後作成する Order には、先頭に以下を追加：

```
# ======================================
# 環境自動チェック（Sakura 共通）
# ======================================
# 参照: ~/virtual-company/skills/sakura-deployment/SKILL.md

[環境確認コード]

# ======================================
# 本体実装
# ======================================

[実装内容]
```

---

**これで、Virtual Company は「自己学習する組織」になります。** 🎯

次からは、同じ失敗は起きません。
