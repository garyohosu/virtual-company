# Virtual Company - AI リソース管理戦略

**対象**: Virtual Company の全 AI エージェント
**目的**: 制限時間・コンテキストを最適に管理
**更新日**: 2026-01-31

---

## 🎯 問題：AI エージェントの制限時間

### 実際に起きた問題

```
Codex CLI:
- 実行時間制限：5 時間
- 複雑な Order は途中で止まる
- 長時間作業に不向き

Claude:
- コンテキスト制限あり
- 長文の結果出力で制限に達する
- リセット必要

ChatGPT:
- API rate limit あり
- 並行実行数制限

Gemini:
- 同様の制限あり

→ 単一 AI では対応不可
```

---

## ✅ 解決策：複数 AI の用途別使い分け

### Virtual Company の推奨構成

| AI | 用途 | 制限 | 強み |
|-----|------|------|------|
| **Claude** | 指示解釈・計画立案 | Context制限 | 高品質・推理 |
| **Codex CLI** | 短時間 Order 実行 | 5h 制限 | SSH 直結 |
| **Gemini CLI** | 中時間 Order 実行 | Codex より長い | Codex の代替 |
| **Genspark** | GitHub・長時間作業 | 制限なし | MCP 統合・無制限 |
| **ChatGPT** | バックアップ・並行実行 | Rate limit | 高速 |

---

## 🔄 実装パターン

### パターン 1: 短時間の Order（目安：1 時間以内）

```
Claude が指示
  ↓
Codex CLI で実行（5h 制限は OK）
  ↓
完了
```

**例**: 
- index.html 作成
- .htaccess 設定
- ファイル配置

### パターン 2: 中時間の Order（目安：1～3 時間）

```
Claude が指示
  ↓
Codex CLI で実行（途中で時間が危ない）
  ↓
Codex が終わらない可能性
  ↓
Gemini CLI に切り替えて実行
```

**例**:
- note 記事テンプレート生成
- 複雑な診断スクリプト

### パターン 3: 長時間の Order（目安：3 時間以上）

```
Claude が指示
  ↓
Genspark で実行（制限なし）
  ↓
完了
```

**例**:
- 大規模な失敗記録ドキュメント化
- 複数ファイル同時処理

### パターン 4: 並行処理（複数 Order を同時実行）

```
複数の Order が同時に必要
  ↓
Codex で Order A
Gemini で Order B
ChatGPT で Order C
を同時実行
  ↓
すべて完了
```

---

## 🛠️ 実装手順

### Step 1: Order ファイルに「推定実行時間」を記入

```markdown
# Order - [名前]

**推定実行時間**: 30 分
**推奨 AI**: Codex CLI
**代替 AI**: Gemini CLI
**制限注意**: なし
```

### Step 2: AI を選んで実行

```bash
# 短時間（30 分以内）→ Codex
codex --yolo order_short_task.md

# 中時間（1-3 時間）→ Codex → Gemini 
gemini-cli run order_medium_task.md

# 長時間（3 時間以上）→ Genspark
genspark --execute order_long_task.md
```

### Step 3: 制限が近づいたら切り替え

```bash
# Codex の残り時間が 25% 以下になったら
# → 次の Order は Gemini で実行
```

---

## 📊 Virtual Company の実績

### 今回の MagicBoxAI 開発

| 作業 | AI | 時間 | 状態 |
|------|-----|------|------|
| Sakura PHP + CGI 実装 | Codex | 1h | ✅ 完了 |
| index.html・.htaccess | Codex | 30min | ✅ 完了 |
| 失敗記録ドキュメント化 | Codex | 2h | ✅ 完了 |
| note 記事テンプレート | **Gemini** | 2h | ⏳ 予定 |
| セキュリティチェック | Genspark | 1h | ⏳ 予定 |
| **合計** | **複数 AI** | **6.5h** | ✅ リスク分散 |

**単一 Codex なら**:
- 5h 制限で途中で止まる
- 不可能

**複数 AI なら**:
- リスク分散
- すべて完了可能

---

## 🎯 ガイドライン

### 各 AI の使い分け判断フロー

```
Order を受け取った
  ↓
推定実行時間を判定
  ↓
  ├─ 30 分以内 → Codex CLI
  │  └─ 理由：最速・安定
  │
  ├─ 30 分～2 時間 → Codex または Gemini
  │  ├─ Codex の残り時間が 50% 以上 → Codex
  │  └─ Codex の残り時間が 50% 未満 → Gemini
  │
  └─ 2 時間以上 → Genspark
     └─ 理由：制限なし・確実
```

### 実行時の注意

```
1️⃣ AI を選ぶ前に、/status で残り時間確認
   $ codex /status

2️⃣ Order に「推定時間」「推奨 AI」を記入

3️⃣ 実行途中で制限に達したら即座に切り替え

4️⃣ 切り替え時は状態を MD で出力
   → 別 AI が引き継げるように
```

---

## 💰 コスト管理

### 複数 AI に課金している理由

```
Codex CLI（月額）:
- 短時間 Order 用

Gemini CLI（月額）:
- Codex の制限を補完
- 制限時間延長

Genspark（月額）:
- 無制限・安定実行
- 長時間作業用

ChatGPT（月額）:
- バックアップ
- 並行実行用

Claude（月額）:
- 高品質指示解釈
- 計画立案

**合計**: 月額 X 万円
**効果**: 24/7 どれか 1 つは使える
```

### ROI 計算

```
3 ヶ月で MagicBoxAI 完成
  ↓
note で月 50 部販売（¥10,000 × 50 = ¥500,000）
  ↓
3 ヶ月で ¥1,500,000 売上
  ↓
複数 AI の月額コスト：簡易計算で月 5 万円
  ↓
**ROI**: 300% 以上
```

---

## 🔄 自動化：AI 選択ロジック

Order に以下を追加：

```bash
# ======================================
# AI リソース自動選択（全 Order 共通）
# ======================================

check_ai_availability() {
    echo "🤖 AI リソース確認中..."
    
    # 1. Codex の残り時間確認
    CODEX_REMAINING=$(codex /status | grep -o '[0-9]*%' | head -1)
    
    if [[ "$CODEX_REMAINING" -gt 50 ]]; then
        echo "✅ Codex を使用（残り時間: $CODEX_REMAINING）"
        return 0
    elif command -v gemini-cli &> /dev/null; then
        echo "✅ Gemini CLI に切り替え（Codex 残り: $CODEX_REMAINING）"
        return 1
    elif command -v genspark &> /dev/null; then
        echo "✅ Genspark に切り替え（最安定）"
        return 2
    else
        echo "❌ 利用可能な AI がありません"
        exit 1
    fi
}

# 使用例
check_ai_availability
AI_CHOICE=$?

case $AI_CHOICE in
    0) codex --yolo order.md ;;
    1) gemini-cli run order.md ;;
    2) genspark --execute order.md ;;
esac
```

---

## 📝 推奨：すべての Order に追加

```markdown
# Order - [名前]

**推定実行時間**: XX 分
**推奨 AI**: [Codex/Gemini/Genspark]
**代替 AI**: [代替手段]

---

[Order 内容]
```

---

## 🎓 教訓

```
単一 AI への依存は危険
  ↓
複数 AI を使い分けることで
  ↓
無制限の作業が可能になる
  ↓
ビジネス継続性が確保される
  ↓
Virtual Company として「24/7 稼働」が実現
```

---

**これが本物の「自己学習する組織」です。**

制限時間を乗り越えるために、複数のリソースを活用する知恵。
